# Dual-layer stress exploratory (2026-08-28)

Exploratory rescore of frozen Gate v2 confirmation sources from seed 1200000. Confirmation artifacts were not rewritten and were not used to retune the mix.

Historical mark-only mix restores the source under Mn-strip and default-ignorable-strip, so detection returns to identity (64/64). Live dual-layer mix (`u034f-ufe00-cc-letter-alt-v1`) keeps C0/C1 residuals after those strips. Weighted-mean GPT-2 SynthID detection stays 0/64 on raw, Mn-strip, and default-ignorable-strip.

This is not a new confirmation corpus and is not a second-model result.
