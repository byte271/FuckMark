# Cycle 7 Stage A evidence

Development-only. Not a formal confirmation. Seed `810000` is the exploratory ledger entry.

Files:

- `fixture-geometry.json` — OffsetTokenizer geometry on hand fixtures, plus detector summaries when scored.
- `decision-geometry-only.json` — classification from fixtures before detector scoring (`PROMISING_DEVELOPMENT` for the collapse-survival *channel*).
- `detector-compare-family1-pre-copula.json` — first durable catalog on the generated corpus (almost no sites).
- `detector-compare.json` — same generated texts rescored after bounded `it's` copulas.
- `decision.json` — Stage A decision after detector attachment: `INSUFFICIENT_EVIDENCE`.

Do not treat these rows as confirmation data. Do not reuse them to retune Cycle 6.


Development-only. Not a formal confirmation. Seed `810000` is the exploratory ledger entry.

Files:

- `fixture-geometry.json` — OffsetTokenizer (byte) geometry on hand fixtures. CI-reproducible.
- `decision-geometry-only.json` — classification before detector scoring.
- `detector-compare.json` — GPT-2 / frozen threshold scoring on a fresh 8-sample corpus (added if the local HF run completes).
- `decision.json` — final Stage A decision after detector attachment when available.

Do not treat these rows as confirmation data. Do not reuse them to retune Cycle 6.
