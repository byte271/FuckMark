# Cycle 7 Stage A evidence

Development-only. Not a formal confirmation. Seed `810000` is the exploratory ledger entry.

Files:

- `fixture-geometry.json` — OffsetTokenizer geometry on hand fixtures, plus detector summaries when scored.
- `decision-geometry-only.json` — classification from fixtures before detector scoring (`PROMISING_DEVELOPMENT` for the collapse-survival *channel*).
- `detector-compare-family1-pre-copula.json` — Family 1 before bounded `it's` copulas (almost no sites).
- `detector-compare-family1-copula.json` — Family 1 after bounded copulas, same generated texts.
- `family2-density.json` — detector-blind candidate counts for catalog v2 on those same texts (no scores used for selection).
- `detector-compare.json` — latest detector rescore of the same generated texts under the current catalog.
- `decision.json` — Stage A decision after detector attachment.

Do not treat these rows as confirmation data. Do not reuse them to retune Cycle 6.
