# Combo stress exploratory seed 1220000 n=64 (2026-08-28)

Exploratory rescore of frozen Gate v2 confirmation sources from seed `1220000`. Confirmation artifacts were not rewritten and were not used to retune the mix.

Historical mark-only and historical dual-layer mixes under Mn-strip then UnicodeSanitizer remain detected (`61/64`). Live triple-layer mix (`u034f-ufe00-cc-me-letter-alt-v1`) stays `0/64` on raw, Mn-strip, default-ignorable strip, UnicodeSanitizer orderings, and the required sanitizer bundle, including Mn then UnicodeSanitizer and bundle then UnicodeSanitizer.

Combined with seeds `1200000` and `1210000` in `evidence/cycle8-combo-stress-exploratory-n192-2026-08-28/`. This is not a new confirmation corpus.
