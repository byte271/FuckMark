# Combo stress exploratory (2026-08-28)

Exploratory rescore of frozen Gate v2 confirmation sources from seed 1200000. Confirmation artifacts were not rewritten and were not used to retune the mix.

Historical mark-only and historical dual-layer mixes restore under Mn-strip then UnicodeSanitizer (`64/64`). Live triple-layer mix (`u034f-ufe00-cc-me-letter-alt-v1`) keeps Me residuals after Mn-strip, default-ignorable-strip, UnicodeSanitizer orderings, and the required sanitizer bundle. Weighted-mean GPT-2 SynthID detection stays `0/64` on those arms.

This is not a new confirmation corpus and is not a second-model result.
