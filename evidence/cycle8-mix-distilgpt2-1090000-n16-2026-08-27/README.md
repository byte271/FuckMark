# Cycle 8 mix DistilGPT2 second-model transfer (seed 1090000)

Status: `HYPOTHESIS`. Reserved before generation. Not mix-freeze confirmation. Not confirmation-scale.

This scores frozen mix apply `u034f-ufe00-letter-alt-v1` on `distilbert/distilgpt2` text generated by Google `synthid_text.SynthIDGPT2LMHeadModel` using DeepMind `DEFAULT_WATERMARKING_CONFIG` (30 keys). Detection is official `synthid_text.logits_processing.SynthIDLogitsProcessor` weighted mean at frozen threshold `0.5570987654320988`.

DistilGPT2 uses different weights from `openai-community/gpt2`. The tokenizer is still GPT-2 BPE (`GPT2TokenizerFast`, vocab 50257). `second_model` is true. `second_configuration` is false: this is not a second keying scheme.

n=16. Identity WM **16/16**. Mix WM **0/16**. Mix UW **0/16**. Identity UW **0/16**. Mix WM max `0.5047997827277791`. Visible pass.

This n=16 look is not added to confirmation-scale families. See `docs/cycle8/mix-second-model-transfer.md`.

`product_authorized` is false. Do not generate `950000`. Do not rewrite mix-freeze confirmation.
