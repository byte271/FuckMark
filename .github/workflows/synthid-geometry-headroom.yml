name: SynthID Geometry Headroom

on:
  workflow_dispatch:
    inputs:
      model:
        description: Hugging Face causal language model
        required: true
        default: openai-community/gpt2
        type: string
      prompt_limit:
        description: Number of built-in prompts
        required: true
        default: "10"
        type: string
      max_new_tokens:
        description: Generated completion tokens per sample
        required: true
        default: "192"
        type: string
      registry:
        description: Transform registry used for the diagnostic
        required: true
        default: mechanism
        type: choice
        options:
          - release
          - development
          - mechanism
      budgets:
        description: Comma-separated operation budgets
        required: true
        default: "1,2,4"
        type: string

permissions:
  contents: read

jobs:
  headroom:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install FuckMark
        run: python -m pip install -e .
      - name: Install CPU Torch
        run: python -m pip install "torch>=2.4,<3" --index-url https://download.pytorch.org/whl/cpu
      - name: Install SynthID experiment dependencies
        run: python -m pip install -r requirements-smoke.txt
      - name: Run detector-free geometry headroom diagnostic
        env:
          INPUT_MODEL: ${{ inputs.model }}
          INPUT_PROMPT_LIMIT: ${{ inputs.prompt_limit }}
          INPUT_MAX_NEW_TOKENS: ${{ inputs.max_new_tokens }}
          INPUT_REGISTRY: ${{ inputs.registry }}
          INPUT_BUDGETS: ${{ inputs.budgets }}
        run: >-
          python -m fuckmark.synthid_geometry_headroom_hf
          --model "$INPUT_MODEL"
          --device cpu
          --prompt-limit "$INPUT_PROMPT_LIMIT"
          --max-new-tokens "$INPUT_MAX_NEW_TOKENS"
          --registry "$INPUT_REGISTRY"
          --budgets "$INPUT_BUDGETS"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: synthid-geometry-headroom-${{ inputs.registry }}
          path: artifacts/
          if-no-files-found: warn
