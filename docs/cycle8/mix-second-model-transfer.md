# Mix second-model transfer (archive pointer)

Historical pointer for the DistilGPT2 n=16 mix transfer (`evidence/cycle8-mix-distilgpt2-1090000-n16-2026-08-27/`). Live triple-layer combo stress on those frozen watermarked sources is `evidence/cycle8-distilgpt2-combo-stress-exploratory-2026-08-28/` (identity 16/16, live triple-layer 0/16 under Mn then UnicodeSanitizer and bundle then UnicodeSanitizer).

Status: `HYPOTHESIS`. Not mix-freeze confirmation. Not confirmation-scale. The tokenizer is still GPT-2 BPE, so this is not an independent tokenizer-family validation.

Status: `HYPOTHESIS`. Not mix-freeze confirmation. Not confirmation-scale. The tokenizer is still GPT-2 BPE, so this is not an independent tokenizer-family validation.

See `docs/research.md` and `docs/limits.md`.
