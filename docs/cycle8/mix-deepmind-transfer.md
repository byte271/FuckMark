# Cycle 8 mix DeepMind 30-key transfer

Status: `HYPOTHESIS`. Confirmation-scale independent configuration. Not mix-freeze confirmation. Not a second model. Not product authorization.

This scores frozen mix apply `u034f-ufe00-letter-alt-v1` on GPT-2 text generated by Google `synthid_text.SynthIDGPT2LMHeadModel` using DeepMind `DEFAULT_WATERMARKING_CONFIG` (30 keys, sampling table 65536). Detection is official `synthid_text.logits_processing.SynthIDLogitsProcessor.compute_g_values` combined with FuckMark weighted mean at frozen threshold `0.5570987654320988`.

It is not Hugging Face `SynthIDTextWatermarkingConfig`. It is not the Cycle 6 nine-key adapter. It is not Mean versus Weighted Mean on the same Hugging Face observations. It is still `openai-community/gpt2`.

Seeds `1060000`, `1070000`, and `1080000` were reserved in `global-seed-ledger-v1` before generation. Seed `920000` n=16 was a prior exploratory look on the same stack and is not part of the 0/192.

## Combined result

| Arm | Detected |
| --- | ---: |
| Identity WM | **189/192** |
| Mix WM | **0/192** |
| Identity UW | **0/192** |
| Mix UW | **0/192** |

Mix WM max `0.5067599700507657`. Visible projection passed on every scored row.

| Seed | Identity WM | Mix WM | Mix UW | Mix WM max |
| ---: | ---: | ---: | ---: | ---: |
| 1060000 | 63/64 | 0/64 | 0/64 | 0.505630 |
| 1070000 | 63/64 | 0/64 | 0/64 | 0.502999 |
| 1080000 | 63/64 | 0/64 | 0/64 | 0.506760 |

`product_authorized` is false. Do not generate `950000`. Do not rewrite `cycle8-mix-confirmation-scorecard-v1`.

See also `docs/cycle8/mix-second-model-transfer.md` for DistilGPT2 n=16 second-model HYPOTHESIS.
