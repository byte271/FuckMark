# Gate v2 (archive pointer)

Historical research write-up for publishability Gate v2. The machine-readable gate is `specs/cycle8/fuckmark-cycle8-publishability-gate-v2.json`. Confirmation scorecard: `evidence/cycle8-gate-v2-confirmation-2026-08-27/`.

At confirmation time some READMEs said the public CLI remained empty. That sentence is historical. Live authorization is `cycle8-product-authorization-v2` / `release-cli-v11` five-layer mix. Frozen Gate v2 confirmation remains the historical mark-only corpus. Do not treat archive "CLI remains empty" lines as current product behavior.

H16 local audit: `evidence/h16-local/README.md`.
